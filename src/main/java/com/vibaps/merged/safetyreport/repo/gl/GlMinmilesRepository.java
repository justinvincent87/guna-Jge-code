package com.vibaps.merged.safetyreport.repo.gl;

import org.springframework.data.jpa.repository.JpaRepository;

import com.vibaps.merged.safetyreport.entity.gl.GlMinmiles;

public interface GlMinmilesRepository extends JpaRepository<GlMinmiles, Long>{

}
